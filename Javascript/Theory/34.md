## JavaScript Objects

🧠 Summary
In this tutorial, you’ll learn what JavaScript objects are, how to access and modify their properties, and how to add, delete, or check for property existence.


🔹 Introduction to JavaScript Objects

A JavaScript object is an unordered collection of key–value pairs.
Each pair is called a property.

Key → always a string (or symbol)

Value → can be anything (string, number, array, function, object, etc.)

Creating an empty object
let empty = {};

Creating an object with properties
let person = {
  firstName: 'John',
  lastName: 'Doe'
};


🔹 Accessing Object Properties
JavaScript provides two notations:

1️⃣ Dot Notation (.)
objectName.propertyName

Example
console.log(person.firstName);
console.log(person.lastName);

2️⃣ Bracket Notation ( [ ] )
objectName['propertyName']

Example:
console.log(person['firstName']);
console.log(person['lastName']);

When property names contain spaces
let address = {
  'building no': 3960,
  street: 'North 1st street',
  state: 'CA',
  country: 'USA'
};

address['building no']; // ✔️ works

Using dot notation will cause an error:

address.'building no'; // ❌ SyntaxError


⚠️ Tip: Avoid using spaces in property names.

Accessing a non-existing property returns undefined
console.log(address.district); // undefined


🔹 Modifying Property Values

Use the assignment operator (=):

let person = {
  firstName: 'John',
  lastName: 'Doe'
};

person.firstName = 'Jane';

console.log(person);
// { firstName: 'Jane', lastName: 'Doe' }


🔹 Adding a New Property

Objects in JavaScript are dynamic — you can add properties anytime:

person.age = 25;


🔹 Deleting a Property

Use the delete operator:

delete person.age;

Accessing it again gives:

console.log(person.age); // undefined


🔹 Checking if a Property Exists

Use the in operator:

propertyName in objectName

Example:

let employee = {
  firstName: 'Peter',
  lastName: 'Doe',
  employeeId: 1
};

console.log('ssn' in employee);        // false
console.log('employeeId' in employee); // true


🧾 Final Summary
| Concept               | Description                               |
| --------------------- | ----------------------------------------- |
| **Objects**           | Collection of key–value pairs             |
| **Access properties** | Dot notation (.) or bracket notation ([]) |
| **Modify property**   | Use `=`                                   |
| **Add property**      | `object.newProp = value`                  |
| **Delete property**   | `delete object.prop`                      |
| **Check existence**   | `'prop' in object`                        |
