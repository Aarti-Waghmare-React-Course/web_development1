String.prototype.toLowerCase()

🧠 Summary
In this tutorial, you’ll learn how to use the JavaScript String.prototype.toLowerCase() method to return a new string with all the characters converted to lowercase.


🔹 Introduction

The toLowerCase() method returns a new string where all alphabetic characters are converted to lowercase.

Syntax:
const newString = str.toLowerCase();


🔹 Example
const s = "JavaScript";
const newStr = s.toLowerCase();

console.log({ newStr });

✅ Output:
{ newStr: 'javascript' }


⚙️ Since JavaScript strings are immutable, toLowerCase() does not modify the original string — it returns a new one.


🔹 Calling toLowerCase() on null or undefined

If you call the method on null or undefined, it will throw a TypeError.

Example:
const findUserById = (id) => {
  if (id > 0) {
    return "admin";
  }
  return null;
};

const user = findUserById(-1).toLowerCase();
console.log({ user });

❌ Error:

TypeError: Cannot read properties of null (reading 'toLowerCase')


✅ Safe Access with Optional Chaining (?.)

To avoid errors, use optional chaining:

const findUserById = (id) => {
  if (id > 0) {
    return "admin";
  }
  return null;
};

const user = findUserById(-1)?.toLowerCase();
console.log({ user });


✅ Output:

{ user: undefined }


🔹 Using toLowerCase() on Non-String Values

The toLowerCase() method can also work on non-string values, because it automatically converts them to strings when called via .call().

Example:
const user = {
  username: "JOE",
  toString() {
    return this.username.toString();
  },
};

const username = String.prototype.toLowerCase.call(user);
console.log(username);

✅ Output:
joe


🧩 Here, the toLowerCase() method first converts the object to a string using its custom toString() method, and then makes all letters lowercase.


🧾 Summary

toLowerCase() returns a new string with all characters in lowercase.

It does not modify the original string.

Throws a TypeError when called on null or undefined.

Works with non-string values via type coercion.