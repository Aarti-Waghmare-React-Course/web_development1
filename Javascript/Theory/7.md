# 🔄 JavaScript Type Conversion & Coercion

## 📘 Summary
In this tutorial, you’ll learn how **JavaScript converts values from one type to another** — both **explicitly (Type Conversion)** and **implicitly (Type Coercion)**.  
This concept is crucial for understanding how JavaScript handles expressions like `'5' + 1` or `10 == '10'`.

---

## ⚙️ Type Conversion (Explicit Conversion)

**Type conversion** happens when you **manually convert** one data type to another using built-in functions.

### 🔢 Convert to Number

Use the `Number()` function or the unary `+` operator.

```javascript
console.log(Number('42'));    // 42
console.log(Number('3.14'));  // 3.14
console.log(Number(''));      // 0
console.log(Number('abc'));   // NaN

console.log(+'10');           // 10
console.log(+'');             // 0
console.log(+true);           // 1
console.log(+false);          // 0

🧠 Note:
Empty strings '' convert to 0

Non-numeric strings convert to NaN

true → 1, false → 0


🔠 Convert to String

Use the String() function or toString() method.
console.log(String(100));       // "100"
console.log(String(true));      // "true"
console.log(String(null));      // "null"

let num = 50;
console.log(num.toString());    // "50"


🟢 Convert to Boolean
Use the Boolean() function.
| Value                                          | Converted To |
| ---------------------------------------------- | ------------ |
| `0`, `NaN`, `""`, `null`, `undefined`, `false` | ❌ `false`    |
| All other values                               | ✅ `true`     |

console.log(Boolean(0));         // false
console.log(Boolean(''));        // false
console.log(Boolean('Hello'));   // true
console.log(Boolean([]));        // true
console.log(Boolean({}));        // true

⚡ Type Coercion (Implicit Conversion)
Type coercion happens automatically when JavaScript converts types behind the scenes during an operation.


🧩 String Coercion
When one operand is a string, JavaScript converts the other to a string and performs concatenation.

console.log('5' + 1);   // "51"
console.log('5' + true); // "5true"
console.log('5' + undefined); // "5undefined"

🧠 Rule:
➡️ The + operator triggers string concatenation if any operand is a string.

➕ Numeric Coercion
When using operators other than +, JavaScript tries to convert values to numbers.
console.log('5' - 1);  // 4
console.log('5' * 2);  // 10
console.log('5' / 2);  // 2.5
console.log('5' - '2'); // 3

🧠 Rule:
➡️ Operators like -, *, /, and % force both operands to numbers.

⚖️ Boolean Coercion in Comparisons

When using == (loose equality), JavaScript coerces values to the same type before comparing.
console.log(1 == '1');   // true
console.log(0 == false); // true
console.log('' == false); // true

🧠 Rule:
➡️ == allows coercion, while === compares without coercion.
console.log(1 === '1');   // false
console.log(0 === false); // false
console.log('' === false); // false

🧠 Special Cases
🪄 null and undefined
console.log(null == undefined); // true
console.log(null === undefined); // false

⚠️ NaN
NaN is not equal to anything, even itself.
console.log(NaN == NaN);  // false
console.log(Number.isNaN(NaN)); // true ✅

📊 Summary Table
| Expression          | Result  | Reason                       |
| ------------------- | ------- | ---------------------------- |
| `'5' + 1`           | `"51"`  | String concatenation         |
| `'5' - 1`           | `4`     | Numeric coercion             |
| `true + 1`          | `2`     | `true` → `1`                 |
| `false + 1`         | `1`     | `false` → `0`                |
| `'10' == 10`        | `true`  | Type coercion by `==`        |
| `'10' === 10`       | `false` | Strict equality, no coercion |
| `null == undefined` | `true`  | Loose equality rule          |
| `[] == false`       | `true`  | `[]` → `''` → `0`            |
| `[1,2] == '1,2'`    | `true`  | Array → String               |


🏁 Key Takeaways

💡 Type Conversion → Done manually using functions like Number(), String(), Boolean().
⚡ Type Coercion → Done automatically by JavaScript during operations.
🧮 + triggers string concatenation, others trigger numeric conversion.
⚖️ Use === instead of == to avoid unexpected results.
🚫 NaN is not equal to anything (even itself).
🧠 null and undefined are loosely equal but not strictly equal.
✅ Always prefer explicit conversion for predictable results.
