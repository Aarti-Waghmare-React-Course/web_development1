# 🚀 JavaScript `String.prototype.split()`

## 🧩 Summary
In this tutorial, you’ll learn how to use the **JavaScript `split()`** method to **split a string into an array of substrings**.

---

## 📘 Introduction
The **`split()`** method divides a string into multiple substrings and returns them as an **array**.

### 🧠 Syntax
string.split([separator[, limit]]);

📖 Parameters
| Parameter   | Description                                                                                       |
| ----------- | ------------------------------------------------------------------------------------------------- |
| `separator` | *(Optional)* Determines where to split the string. Can be a **string** or **regular expression**. |
| `limit`     | *(Optional)* A **positive integer** specifying the number of substrings to include in the result. |


🧩 Notes:

If you omit the separator or it’s not found, the method returns an array containing the original string.

If limit = 0, it returns an empty array.

If limit = 1, it returns an array with only the original string.


💡 Example 1: Splitting a String into Words
The following example splits a string into words using a space ' ' as the separator:

let str = 'JavaScript String';
let substrings = str.split(' ');

console.log(substrings);
✅ Output
["JavaScript", "String"]

🧠 Explanation:
The space ' ' is removed, and the string is divided into an array of two words.


💡 Example 2: Limiting the Number of Substrings
The split() method can take a second argument to limit the number of substrings returned.

let str = 'JavaScript String';
let substrings = str.split(' ', 2);

console.log(substrings);
✅ Output
["JavaScript", "String"]

🧠 Explanation:
Even though the string has more potential splits, the limit (2) stops it from splitting further.


💡 Example 3: Splitting with Regular Expressions
You can also use regular expressions as separators.

let paragraph = 'Good Morning! How are you? This is John. John is my friend.';
let sentences = paragraph.split(/[!,?,.]/);
console.log(sentences);
✅ Output
["Good Morning", " How are you", " This is John", " John is my friend", ""]

🧠 Explanation:
The regex /[!,?,.]/ matches punctuation marks and splits the string wherever these characters appear.


💡 Example 4: Including Capturing Groups
If the regular expression includes capturing parentheses (), the matched results are also included in the output array.

let paragraph = 'Good Morning! How are you? This is John. John is my friend.';
let sentences = paragraph.split(/([!,?,.])/);
console.log(sentences);
✅ Output
["Good Morning", "!", " How are you", "?", " This is John", ".", " John is my friend", ".", ""]

🧠 Explanation:
The punctuation marks (!, ?, .) are now part of the result array because they were captured by the regex.


🧾 Summary Table
| Feature            | Description                                                                                              |
| ------------------ | -------------------------------------------------------------------------------------------------------- |
| 🔹 Method          | `split(separator, limit)`                                                                                |
| 🧱 Returns         | An **array** of substrings                                                                               |
| ✂️ Separator       | Can be a **string** or **RegExp**                                                                        |
| 🔢 Limit           | Restricts number of splits                                                                               |
| 🧩 Related Methods | [`String.prototype.slice()`](./String-slice.md), [`String.prototype.substring()`](./String-substring.md) |


🔗 Navigation
🔙 [Back to String.padEnd()](23.md)

⏭️ [Next: JavaScript String.subString() →](25.md)

