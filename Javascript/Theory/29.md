# String.prototype.replace()

### 🧠 Summary  
In this tutorial, you’ll learn how to use the **JavaScript `String.prototype.replace()`** method to return a new string with one or more matches replaced by a new string.

---

## 🔍 Introduction to the JavaScript `replace()` Method

The **`replace()`** method returns a **new string** where the **first occurrence** of a substring is replaced by a new one.

**Syntax:**
```javascript
let newStr = str.replace(substr, newSubstr);
Returns a new string without modifying the original.

To replace all occurrences, use the replaceAll() method instead.


💡 JavaScript replace() Method Examples
1️⃣ Basic Example
let str = "JS will, JS will rock you!";
let newStr = str.replace("JS", "JavaScript");
console.log({ newStr });

Output:
{ newStr: 'JavaScript will, JS will rock you!' }

👉 Only the first occurrence of "JS" is replaced.


2️⃣ Replace All Occurrences with replaceAll()
let str = "JS will, JS will rock you!";
let newStr = str.replaceAll("JS", "JavaScript");

console.log({ newStr });

Output:
{ newStr: 'JavaScript will, JavaScript will rock you!' }


3️⃣ Using Regular Expressions
You can use regular expressions to replace all occurrences by using the global (g) flag.

let str = "JS will, JS will rock you!";
let newStr = str.replace(/JS/g, "JavaScript");

console.log({ newStr });

Output:
{ newStr: 'JavaScript will, JavaScript will rock you!' }


4️⃣ Case-Insensitive Replacement
Use the ignore case (i) flag with the global (g) flag for case-insensitive replacements:

let str = "JS will, Js will rock you!";
let newStr = str.replace(/JS/gi, "JavaScript");

console.log({ newStr });
Output:
{ newStr: 'JavaScript will, JavaScript will rock you!' }

⚠️ replaceAll() is case-sensitive, so "Js" would not match "JS".

let str = 'JS will, Js will rock you!';
let newStr = str.replaceAll('JS', 'JavaScript');

console.log({ newStr });

Output:
{ newStr: 'JavaScript will, Js will rock you!' }


🧠 Using a Replacement Function
Instead of passing a replacement string, you can pass a function to dynamically replace matches.

Syntax:
let newStr = str.replace(substr | regexp, replacer);

The replacer function is called for each match, and its return value is used as the replacement string.


Function Parameters:

function replacer(match, p1, p2, ..., offset, string)
match → the matched substring

p1, p2, ...pn → capture groups (if any)

offset → index position of the match

string → the entire input string


Example: Replacing Words with Uppercase
let str = "I like to eat, eat, eat apples and bananas";
let re = /apples|bananas/gi;

let newStr = str.replace(re, (match) => { 
  console.log({ match }); 
  return match.toUpperCase();
});

console.log(newStr);

Output:
{ match: "apples" }
{ match: "bananas" }
I like to eat, eat, eat APPLES and BANANAS


🧾 Summary
✅ replace() → replaces the first match and returns a new string.

✅ replaceAll() → replaces all matches (case-sensitive).

✅ Use regex with global flag (/g) to replace all matches.

✅ Use a replacement function for dynamic or conditional replacements.


➡️ [Next: String.prototype.replaceAll() →](30.md)