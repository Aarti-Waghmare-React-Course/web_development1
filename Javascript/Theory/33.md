# String.prototype.repeat()

### 🧠 Summary  
In this tutorial, you’ll learn how to use the **JavaScript `repeat()` method** to repeat a string a number of times.

---

## 🔍 Introduction to the JavaScript String `repeat()` Method

The **`String.prototype.repeat()`** method returns a **new string** that repeats the original string a specified number of times.

**Syntax:**
```javascript
str.repeat(count)
count → An integer that specifies how many times to repeat the string.
Must be ≥ 0 and < +Infinity.

⚠️ If count is 0, repeat() returns an empty string.
If count is negative or Infinity, it throws a RangeError.

💡 The repeat() method does not modify the original string — it returns a new one.


💡 JavaScript repeat() Method Examples
1️⃣ Basic Example
let result = '*'.repeat(1);
console.log({ result });

result = '*'.repeat(3);
console.log({ result });

result = '*'.repeat(0);
console.log({ result });

Output:
{ result: '*' }
{ result: '***' }
{ result: '' }


2️⃣ Negative Count Example
Passing a negative value throws a RangeError:

let result = '*'.repeat(-1);

Output:
RangeError: Invalid count value


3️⃣ Using repeat() with a Non-String Object
The repeat() method is generic — it can work with any object that has a toString() method:

const message = {
  toString() {
    return 'Hi';
  },
};

const result = String.prototype.repeat.call(message, 3);
console.log(result);

Output:
HiHiHi


🧾 Summary
The repeat() method repeats a string count times and returns a new string.

Throws a RangeError if count < 0 or is Infinity.

It’s generic — can be used with non-string objects via call() or apply().
