# 📌 JavaScript `Array.prototype.push()`

### **Summary**
In this tutorial, you’ll learn how to use the JavaScript **`push()` method** to append one or more elements to the end of an array.

---

## 🚀 Introduction

The **`Array.prototype.push()`** method:

- Adds **one or more elements** to the **end** of an array  
- **Modifies the original array**
- Returns the **new length** of the array

### ✅ Syntax

push(newElement)
push(newElement1, newElement2)
push(newElement1, newElement2, ..., newElementN)
If you want to append elements without modifying the original array, use:

concat([newElement1, newElement2, ...])

🧪 Examples

1️⃣ Append One Element
let numbers = [10, 20, 30];

const length = numbers.push(40);

console.log({ numbers });
console.log({ length });
Output:

{ numbers: [10, 20, 30, 40] }
{ length: 4 }

2️⃣ Add Multiple Elements
let numbers = [10, 20, 30];

const length = numbers.push(40, 50);

console.log({ numbers });
console.log({ length });
Output:

{ numbers: [10, 20, 30, 40, 50] }
{ length: 5 }

3️⃣ Append Elements of One Array to Another
❌ Using Loop + push()

let colors = ['red', 'green', 'blue'];
let cmyk = ['cyan', 'magenta', 'yellow', 'back'];

for (const color of cmyk) {
  colors.push(color);
}

console.log(colors);

✅ Using Spread Operator (ES6)

let colors = ['red', 'green', 'blue'];
let cmyk = ['cyan', 'magenta', 'yellow', 'back'];

colors.push(...cmyk);

console.log({ colors });


🧩 Using push() With Array-Like Objects
Since push() is generic, you can use it on objects that look like arrays.

Example:
let greetings = {
  0: 'Hi',
  1: 'Hello',
  length: 2,
  append(message) {
    [].push.call(this, message);
  },
};

greetings.append('Howdy');
greetings.append('Bonjour');

console.log(greetings);
Output:

{
  '0': 'Hi',
  '1': 'Hello',
  '2': 'Howdy',
  '3': 'Bonjour',
  length: 4,
  append: [Function: append]
}

🛠 Updated Version Supporting Multiple Messages

let greetings = {
  0: 'Hi',
  1: 'Hello',
  length: 2,
  append() {
    [].push.call(this, ...arguments);
  },
};

greetings.append('Howdy', 'Bonjour');

console.log(greetings);


📝 Summary
✔ Use push() to append one or more elements to the end of an array.

✔ It modifies the original array.

✔ It also works with array-like objects using .call() or .apply().

✔ Use concat() if you don’t want to mutate the array.

