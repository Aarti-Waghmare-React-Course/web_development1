# 🚀 JavaScript `String.prototype.substring()`

## 🧩 Summary
In this tutorial, you’ll learn how to use the **JavaScript `substring()`** method to **extract a substring** from a string.

---

## 📘 Introduction
The **`substring()`** method returns a portion of a string between two specified indexes.

### 🧠 Syntax
str.substring(startIndex [, endIndex])

📖 Parameters
| Parameter    | Description                                                                      |
| ------------ | -------------------------------------------------------------------------------- |
| `startIndex` | The index of the **first character** to include in the returned substring.       |
| `endIndex`   | *(Optional)* The index of the **first character to exclude** from the substring. |


🧩 Notes:

If you omit endIndex, the substring extends to the end of the string.

If startIndex === endIndex, an empty string is returned.

If startIndex > endIndex, their values are swapped automatically.

Negative or out-of-range values are treated as 0 or string.length.

If either argument is NaN, it’s treated as 0.


💡 Example 1: Extracting from the Beginning

Extract a substring from index 0 to 10.

let str = 'JavaScript Substring';
let substring = str.substring(0, 10);

console.log(substring);

✅ Output
JavaScript

🧠 Explanation:
Returns characters from index 0 to 9, excluding index 10.


💡 Example 2: Extracting to the End

If you omit endIndex, the method extracts the substring to the end.

let str = 'JavaScript Substring';
let substring = str.substring(11);

console.log(substring);

✅ Output
Substring

🧠 Explanation:
It starts from index 11 and continues to the end of the string.


💡 Example 3: Extracting the Domain from an Email

You can combine substring() with indexOf() to extract meaningful parts of a string.

let email = 'john.doe@gmail.com';
let domain = email.substring(email.indexOf('@') + 1);

console.log(domain);

✅ Output
gmail.com


🧠 How it works:

indexOf('@') returns the position of the @ symbol.

substring() extracts everything after that position.


🧾 Summary Table
| Feature                | Description                                                                                        |
| ---------------------- | -------------------------------------------------------------------------------------------------- |
| 🔹 Method              | `substring(startIndex, endIndex)`                                                                  |
| 🧱 Returns             | Portion of string between the two indices                                                          |
| ✂️ Behavior            | Excludes `endIndex` character                                                                      |
| ⚙️ Swaps automatically | If `startIndex > endIndex`                                                                         |
| 🧩 Related Methods     | [`String.prototype.slice()`](./String-slice.md), [`String.prototype.substr()`](./String-substr.md) |


🔗 Navigation

🔙 [Back to String.split()](24.md)

⏭️ [Next: JavaScript String.slice() →](26.md)