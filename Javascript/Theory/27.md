# String.prototype.concat()

### 🧠 Summary  
In this tutorial, you’ll learn how to use the **JavaScript `concat()` method** to concatenate strings.

---

## 🔍 Introduction to the JavaScript String `concat()` Method

The `String.prototype.concat()` method accepts a list of strings and returns a **new string** that contains the combined strings:

string.concat(str1, [...strN]);
If the arguments are not strings, the concat() converts them to strings before concatenation.

⚡ It’s generally recommended to use the + or += operator for string concatenation as they offer better performance.


💡 JavaScript String concat() Examples
1️⃣ Concatenating Strings
The following example uses the concat() method to concatenate multiple strings:

let greeting = 'Hi';
let message = greeting.concat(' ', 'John');
console.log(message);

Output:
Hi John


2️⃣ Concatenating an Array of Strings
This example demonstrates concatenating strings from an array:

let colors = ['Blue', ' ', 'Green', ' ', 'Teal'];
let result = ''.concat(...colors);
console.log(result);

Output:
Blue Green Teal

ℹ️ The ... before the colors array is the spread operator — it unpacks the array elements.


3️⃣ Concatenating Non-String Arguments
The concat() method automatically converts non-string values (like numbers) to strings before concatenation:

let str = ''.concat(1, 2, 3);
console.log(str);

Output:
123


🧾 Summary
The concat() method joins multiple strings and returns a new one.

It automatically converts non-string values to strings.

⚠️ Prefer + or += for better performance in real-world scenarios.

✅ [Next: Template Literals →](28.md)