# 🔁 JavaScript `String.prototype.lastIndexOf()`

## 🧩 Summary
In this tutorial, you’ll learn how to use the **JavaScript `String.lastIndexOf()`** method to find the **last occurrence** of a substring within a string.

---

## 📘 Introduction
The **`lastIndexOf()`** method returns the **index of the last occurrence** of a substring (`substr`) in a string (`str`).

let index = str.lastIndexOf(substr [, fromIndex]);


📎 Key Points:
Returns -1 if the substring is not found.
The search is case-sensitive.
By default, the search starts backward from the end of the string.
The optional fromIndex argument defines where to start searching backward.


🧠 To find the first occurrence of a substring, use indexOf().


⚙️ fromIndex Parameter Details
| Case             | Behavior                                                |
| ---------------- | ------------------------------------------------------- |
| **Omitted**      | Starts from the end of the string (default `+Infinity`) |
| **> str.length** | Searches the entire string                              |
| **< 0**          | Treated as `0` (search stops immediately)               |



💡 Example 1: Basic Usage

let str = 'JavaScript';
let index = str.lastIndexOf('a');

console.log(index);
✅ Output
3

🧩 Explanation
The substring 'a' last appears at index 3 in the string 'JavaScript'.


💡 Example 2: Using fromIndex
You can specify a fromIndex to start searching backward from a specific position:

let str = "JavaScript";
let index = str.lastIndexOf("a", 2);

console.log({ index });
✅ Output
{ index: 1 }

🧠 Explanation
The search starts backward from index 2, so it finds the 'a' at index 1 instead of 3.


💡 Example 3: Case-Sensitive Search

let str = "Hello, World!";
let substr = "L";

let index = str.lastIndexOf(substr);

console.log({ index });
✅ Output
{ index: -1 }

🧩 Explanation
The method is case-sensitive, and since "L" (uppercase) does not match "l" (lowercase) in "Hello, World!", the result is -1.


💡 Example 4: Case-Insensitive Search
To perform a case-insensitive search, convert both strings to lowercase (or use toLocaleLowerCase()):

let str = "Hello, World!";
let substr = "L";

let index = str.toLocaleLowerCase().lastIndexOf(substr.toLocaleLowerCase());

console.log({ index });
✅ Output
{ index: 10 }

🧠 Explanation
Both the string and substring are converted to lowercase, allowing "l" to match successfully at index 10.


🧾 Summary
String.lastIndexOf() returns the index of the last occurrence of a substring in a string.

Returns -1 if the substring is not found.

Search is case-sensitive by default.

Use toLowerCase() for case-insensitive searches.

Useful for reverse string searches or validating last positions of specific characters.


🔗 Navigation

🔙 [Back to String.indexOf()](14.md)

⏭️ [Next: String.includes() →](16.md)