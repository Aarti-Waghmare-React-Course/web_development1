# 🔍 JavaScript `String.prototype.search()`

## 🧩 Summary
In this tutorial, you’ll learn how to use the **JavaScript `String.search()`** function to search for a match of a string with a **regular expression** and return the **index of the first match**.

---

## 📘 Introduction
The **`search()`** method matches a string against a **regular expression** and returns the **index** of the first match.


let index = str.search(regexp);
If the search finds no match, it returns -1.

If you pass a value that is not a regular expression, JavaScript automatically converts it using:


RegExp(regexp);
🧠 Syntax
str.search(regexp)

Parameters
regexp: A regular expression object or any object with a Symbol.search() method.

Returns
The index of the first match, or -1 if no match is found.

💡 Example 1: Basic Usage
let re = /[A-Z]/;
let str = 'hi There! How are you?';
let index = str.search(re);

console.log(index);
✅ Output
3

🧩 Explanation
It returns 3, which is the index of the capital letter T in the string.

💡 Example 2: When No Match is Found
let re = /[0-9]/;
let str = 'Hello, JavaScript!';
let index = str.search(re);

console.log(index);
✅ Output
-1

🧩 Explanation
There are no digits in the string, so the method returns -1.

💡 Example 3: Using Symbol.search with a Custom Object
class EmailDomainChecker {
  constructor(domain) {
    this.domain = domain;
  }

  [Symbol.search](email) {
    if (!email.includes("@")) {
      return -1;
    }
    const [, domain] = email.split("@");
    return domain === this.domain ? 1 : -1;
  }
}

const js = new EmailDomainChecker("javascripttutorial.net");

let isJsEmail = "hi@javascripttutorial.net".search(js) === 1;
console.log({ isJsEmail });

isJsEmail = "test@test.com".search(js) === 1;
console.log({ isJsEmail });

✅ Output

{ isJsEmail: true }
{ isJsEmail: false }


🧠 How It Works
The EmailDomainChecker class defines a custom Symbol.search() method.

The method checks if the email’s domain matches the one provided.

When the search() method is called, it invokes this custom method instead of a regular regex search.


🧾 Summary
The String.search() method searches for the first match in a string using a regular expression.

Returns the index of the match or -1 if not found.

Can also work with custom objects implementing Symbol.search.


🔗 Navigation
🔙 [Back to JavaScript String Methods](./12.md)

⏭️ [Next: indexOf() →](./13.md)