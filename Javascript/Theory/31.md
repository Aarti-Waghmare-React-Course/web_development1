String.prototype.toUpperCase()

🧠 Summary
In this tutorial, you’ll learn how to use the JavaScript String.prototype.toUpperCase() method to return a new string with all the characters converted to uppercase.


🔹 Introduction
The toUpperCase() method returns a new string where all alphabetic characters are converted to uppercase.

Syntax:
const newStr = str.toUpperCase();


🔹 Example
const str = "JavaScript";
const newStr = str.toUpperCase();

console.log({ newStr });

✅ Output:
{ newStr: 'JAVASCRIPT' }


Note: Strings in JavaScript are immutable, so toUpperCase() does not modify the original string — it returns a new one.


🔹 Calling toUpperCase() on undefined or null

If you try to call toUpperCase() on null or undefined, you’ll get a TypeError.

Example:
const getUserRank = (id) => {
  if (id > 0) {
    return "Standard";
  }
};

const rank = getUserRank(-1).toUpperCase();
console.log({ rank });


❌ Error:

TypeError: Cannot read properties of undefined (reading 'toUpperCase')


✅ Safe Access with Optional Chaining (?.)

You can safely call it using optional chaining:

const getUserRank = (id) => {
  if (id > 0) {
    return "Standard";
  }
};

const rank = getUserRank(-1)?.toUpperCase();
console.log({ rank });

✅ Output:

{ rank: undefined }


🔹 Using toUpperCase() on Non-String Values

The toUpperCase() method can also work on non-string values, because it first converts them to a string internally.

Example:
const completed = true;
const result = String.prototype.toUpperCase.call(completed);

console.log(result);

✅ Output:
TRUE


🧩 Summary

Use toUpperCase() to convert all characters in a string to uppercase.

It does not modify the original string (returns a new one).

Avoid calling it on null or undefined.

It can work on non-string values via type coercion.