# 🚀 JavaScript `String.prototype.slice()`

## 🧩 Summary
In this tutorial, you will learn how to use the **JavaScript `String.slice()`** method to **extract a substring** from a string.

---

## 📘 Introduction
The **`slice()`** method extracts a portion of a string and returns it as a new substring **without modifying the original string**.


### 🧠 Syntax
string.slice(start, end)


📖 Parameters
| Parameter | Description                                                                                                          |
| --------- | -------------------------------------------------------------------------------------------------------------------- |
| `start`   | Zero-based index where extraction begins.                                                                            |
| `end`     | *(Optional)* Zero-based index **before which** extraction ends (the character at this position is **not included**). |


⚙️ start Parameter Behavior
✅ Example 1: Extract from Index
const str = 'Hello';
const substr = str.slice(3);

console.log({ substr });


Output:

{ substr: 'lo' }


✅ Example 2: Negative Start

If start is negative, counting begins from the end of the string.

const str = 'Hello';
const substr = str.slice(-3);
console.log({ substr });

Output:
{ substr: 'llo' }


✅ Example 3: Start Omitted
If start is omitted or not a number, slicing starts from index 0.

const str = 'Hello';
const substr = str.slice();
console.log({ substr });

Output:
{ substr: 'Hello' }


✅ Example 4: Start Beyond Length
If start ≥ string.length, the method returns an empty string.

const str = 'Hello';
const substr = str.slice(5);
console.log({ substr });

Output:
{ substr: '' }


⚙️ end Parameter Behavior
✅ Example 5: Basic End Index
const str = 'Hello';
const substr = str.slice(0, 2);

console.log({ substr });

Output:
{ substr: 'He' }


✅ Example 6: Negative End
If end is negative, it’s treated as string.length + end.

const str = 'Hello';
const substr = str.slice(0, -2);

// str.length = 5
// str.length + end = 5 + (-2) = 3
console.log({ substr });

Output:
{ substr: 'Hel' }


✅ Example 7: End Beyond Length

If end is greater than string length, extraction continues to the end.

const str = 'Hello';
const substr = str.slice(2, 6);
console.log({ substr });

Output:

{ substr: 'llo' }


✅ Example 8: End Omitted

If end is omitted, extraction continues to the end.

const str = 'Hello';
const substr = str.slice(3);
console.log({ substr });

Output:

{ substr: 'lo' }


✅ Example 9: End < Start

If end is before start, the method returns an empty string.

const str = 'Hello';
const substr = str.slice(3, 2);
console.log({ substr });

Output:
{ substr: '' }


💡 Practical Example — Extract Email Local Part
let email = 'john@example.com';
let localPart = email.slice(0, email.indexOf('@'));
console.log(localPart);

Output:
john

🧠 Explanation:

indexOf('@') finds the @ position.

slice(0, indexOf('@')) extracts the text before it.


🧾 Summary Table
| Feature             | Description                                                              |
| ------------------- | ------------------------------------------------------------------------ |
| 🔹 Method           | `slice(start, end)`                                                      |
| ✂️ Returns          | Substring between `start` and `end`                                      |
| ⚙️ Excludes         | Character at `end` index                                                 |
| 🧮 Negative Support | Yes (`from the end`)                                                     |
| 🔗 Related Methods  | [`substring()`](./String-substring.md), [`substr()`](./String-substr.md) |


🔗 Navigation

🔙 [Back to String.substring()](25.md)

⏭️ [Next: JavaScript String.concate() →](27.md)
