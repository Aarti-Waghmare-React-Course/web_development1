# 🔍 JavaScript `String.prototype.indexOf()`

## 🧩 Summary
In this tutorial, you’ll learn how to use the **JavaScript `String.indexOf()`** method to find the index of the **first occurrence of a substring** within a string.

---


## 📘 Introduction
The **`indexOf()`** method returns the **index of the first occurrence** of a substring (`substr`) in a string (`str`).


let index = str.indexOf(substr [, fromIndex]);
If the substring is not found, it returns -1.

The search is case-sensitive.

The optional parameter fromIndex specifies the position in the string at which to start the search (default is 0).

🧠 To find the last occurrence of a substring, use lastIndexOf().


💡 Example 1: Basic Usage

let str = 'finding substring in string';
let index = str.indexOf('str');

console.log(index);
✅ Output
11

🧩 Explanation
The substring 'str' first appears at index 11 in the given string.

💡 Example 2: Counting Occurrences of a Substring
You can use a while loop and the indexOf() method to count how many times a substring appears in a string.

let str = "You do not know what you do not know until you know.";
let substr = "know";

let count = 0;
let index = str.indexOf(substr);

while (index !== -1) {
  count++;
  index = str.indexOf(substr, index + 1);
}

console.log({ count });
✅ Output
{ count: 3 }


🧠 How it Works:
Find the first occurrence of "know" in the string.

Increment the counter.

Continue searching from the next position (index + 1).

Stop when no more matches are found (indexOf returns -1).


💡 Example 3: Case-Sensitive Search

let str = "JS indexOf";
let substr = "js";

let index = str.indexOf(substr);

console.log({ index }); // -1
✅ Output
{ index: -1 }

🧩 Explanation
indexOf() is case-sensitive, so "JS" and "js" are considered different.


💡 Example 4: Case-Insensitive Search
To perform a case-insensitive search, convert both the string and the substring to lowercase before comparing:


let str = "JS indexOf";
let substr = "js";

let index = str.toLocaleLowerCase().indexOf(substr.toLocaleLowerCase());

console.log(index);
✅ Output
0


🧠 Explanation
Both strings are converted to lowercase before searching, so the first match is found at index 0.


🧾 Summary
String.indexOf() returns the index of the first occurrence of a substring.

Returns -1 if not found.

Search is case-sensitive by default.

Use toLowerCase() or toLocaleLowerCase() for case-insensitive searches.


🔗 Navigation
🔙 [Back to JavaScript String Methods](./12.md)


