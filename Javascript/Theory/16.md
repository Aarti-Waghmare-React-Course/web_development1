# 🔍 JavaScript `String.prototype.includes()`

## 🧩 Summary
In this tutorial, you’ll learn how to use the **JavaScript `String.includes()`** method to **check if a string contains a specific substring**.

---

## 📘 Introduction
The **`includes()`** method performs a **case-sensitive** search to determine if a substring (`searchString`) exists within a string.
string.includes(searchString [, position]);


📎 Key Points:
Returns true if the substring is found.
Returns false otherwise.
The optional position parameter specifies where the search starts (default is 0).


🧠 If you need the index/position of the substring instead of a true/false result, use indexOf().


💡 Example 1: Basic Usage
let email = "hi@javascripttutorial.net";
const result = email.includes("@");

console.log({ result });
✅ Output
{ result: true }

🧠 Explanation
The substring '@' exists in the string, so includes() returns true.


💡 Example 2: Case Sensitivity
let str = "JavaScript String";
const result = str.includes("Script");

console.log({ result });
✅ Output
{ result: true }

Now, observe the following example:

let str = "JavaScript String";
const result = str.includes("script");

console.log({ result });
❌ Output
false

🧠 Explanation
The includes() method is case-sensitive, so "Script" and "script" are considered different.


💡 Example 3: Using the position Parameter
You can specify where to start searching within the string:

let str = "JavaScript String";
const result = str.includes("S", 10);

console.log({ result });
✅ Output
{ result: true }

🧠 Explanation
The method starts searching from index 10 and finds "S" later in the string, returning true.


🧾 Summary
✅ includes() checks whether a substring exists within a string.

🔠 The search is case-sensitive.

⚙️ Use the optional position parameter to define a starting point.

🔍 Use indexOf() if you need the index instead of a boolean value.


🔗 Navigation
🔙 [Back to lastIndexOf()](15.md)

⏭️ [Next: startsWith() →](17.md)