🔹 What is JavaScript?
A programming language used to make web pages interactive.

Works alongside HTML and CSS.

Runs in browsers through JavaScript Engines like:

V8 (Chrome)

SpiderMonkey (Firefox)

JavaScriptCore (Safari)


🔹 Main Components of JavaScript in the Browser
ECMAScript – Core language features (syntax, types, etc.)

DOM (Document Object Model) – Interact with HTML elements.

BOM (Browser Object Model) – Interact with the browser (e.g., alert, location).


🔹 Client-Side vs Server-Side JavaScript
Client-Side: Runs in browser (e.g., form validation, animations).

Server-Side: Runs on servers via Node.js (e.g., databases, file systems).


🔹 JavaScript History
Created in 1995 by Brendan Eich at Netscape.

Originally named Mocha, then LiveScript, and finally JavaScript.

Microsoft introduced JScript for Internet Explorer.

Led to standardization by ECMA (ECMAScript) in 1997 (ECMA-262).


🔹 JavaScript Basics
✅ Variables
var – Old way (function-scoped).
let, const – ES6 (block-scoped; preferred).

let x = 10;
const y = 20;


✅ Functions
function add(a, b) {
  return a + b;
}
let result = add(10, 20);
console.log(result); // 30


✅ Conditions
if (b === 0) {
  throw 'Division by zero';
}


✅ Arrays
let items = [1, 2, 3];
console.log(items.length); // 3

for (let i = 0; i < items.length; i++) {
  console.log(items[i]);
}

// OR (ES6)
for (let item of items) {
  console.log(item);
}


🔹 JavaScript Engine
Interprets and executes JS code.

Modern engines use JIT (Just-In-Time) compilation for better performance.


✅ Key Takeaways
JavaScript is essential for web interactivity.

Standardized via ECMAScript.

Works both on browsers (client-side) and servers (via Node.js).

Learn basic syntax, variables, functions, conditions, and loops to get started.

