# 🧵 JavaScript String

### 🧠 Summary  
In this tutorial, you’ll learn about the **JavaScript string** primitive type and how to use it to define, manipulate, and compare strings.

---

## 📖 Introduction to JavaScript Strings

JavaScript **strings** are **primitive values** and **immutable** — meaning any modification creates a **new string**, leaving the original unchanged.

### ✍️ Creating Strings

You can define strings using **single quotes (`'`)**, **double quotes (`"`)**, or **template literals (` `)**.

let str = 'Hi';
let greeting = "Hello";


// ES6 Template Literal
let name = `John`;

💬 Template Literals
Template literals (introduced in ES6) let you:
Use both single and double quotes inside a string without escaping.

Perform string interpolation (embed variables or expressions).

let name = 'John';
let message = `Hi, I'm ${name}.`;

console.log(message);
🧩 Output:
Hi, I'm John.


🧱 Escaping Special Characters
Use the backslash (\) to escape special characters inside strings.

Character	             Meaning
\n	                     New line
\r\n	                 Windows line break
\t	                     Tab
\\	                     Backslash
\'	                     Single quote

Example:

let str = 'I\'m a string!';


📏 Getting String Length
Use the .length property to get the number of characters in a string.

let str = "Good Morning!";
console.log(str.length); // 13


🔍 Accessing Characters
You can access characters in a string using array-like bracket notation ([]) with zero-based indexing.

let str = "Hello";
console.log(str[0]); // "H"
console.log(str[str.length - 1]); // "o"


🔗 Concatenating Strings
You can join strings using the + operator or the += operator.

let name = 'John';
let str = 'Hello ' + name;
console.log(str); // "Hello John"


Building strings progressively:

let className = 'btn';
className += ' btn-primary';
className += ' none';

console.log(className); // "btn btn-primary none"


🔄 Converting Values to Strings
You can convert non-string values to strings using any of the following:

String(value)
value.toString()
"" + value

⚠️ The toString() method does not work for undefined and null.

Example:

let status = false;
let str = status.toString(); // "false"
let back = Boolean(str); // true


🧠 Why true?
Because "false" is a non-empty string, and non-empty strings are truthy.

✅ Only the empty string ("") evaluates to false when converted to a boolean.


⚖️ Comparing Strings
Use comparison operators (>, <, >=, <=, ==) to compare strings lexicographically (based on Unicode values).

let result1 = 'a' < 'b';
console.log(result1); // true

let result2 = 'a' < 'B';
console.log(result2); // false


💬 Note: JavaScript compares based on character code, not dictionary order.
Uppercase letters come before lowercase ones.


🏁 Summary
✅ JavaScript strings are primitive and immutable.
✅ Strings can be created with ', ", or `.
✅ .length returns the string length.
✅ Use + or += for concatenation.
✅ Convert values using String(), toString(), or concatenation.
✅ Comparison uses Unicode order (case-sensitive).

