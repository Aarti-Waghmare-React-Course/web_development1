# 🚀 JavaScript `String.prototype.endsWith()`

## 🧩 Summary
In this tutorial, you’ll learn how to use the **JavaScript `String.endsWith()`** method to check whether a string **ends with** a specific substring.

---

## 📘 Introduction
The **`endsWith()`** method determines whether a string ends with the characters of a specified substring and returns a boolean value (`true` or `false`).

### 🧠 Syntax
string.endsWith(searchString [, endPosition]);

⚙️ Parameters:
searchString → The substring to be searched for at the end of the string.

endPosition (optional) → Defines the position up to which the string is considered.
Defaults to the string’s length.


📜 Notes:
Always performs a case-sensitive comparison.

To check whether a string starts with a substring, use startsWith().


💡 Example 1: Basic Usage
const s = "JavaScript is awesome";
const result = s.endsWith("awesome");

console.log({ result });
✅ Output
{ result: true }

🧠 Explanation:
The string "JavaScript is awesome" ends with "awesome", so endsWith() returns true.


💡 Example 2: Case-Sensitive Search
const s = "JavaScript is awesome";
const result = s.endsWith("Awesome");

console.log({ result });
❌ Output
{ result: false }

🧠 Explanation:
The method performs a case-sensitive check — "awesome" ≠ "Awesome", so it returns false.


💡 Example 3: Using the endPosition Parameter
const s = "JavaScript is awesome";
const result = s.endsWith("Script", 10);

console.log({ result });
✅ Output
{ result: true }

🧠 Explanation:
Here, endPosition is set to 10, so the string "JavaScript" is checked — it ends with "Script", returning true.


🧾 Summary
| Feature                    | Description                              |
| -------------------------- | ---------------------------------------- |
| ✅ Returns `true` / `false` | Checks if a string ends with a substring |
| 🔠 Case-sensitive          | `"awesome"` ≠ `"Awesome"`                |
| ⚙️ Optional `endPosition`  | Defines where to stop checking           |
| 🔁 Related Method          | [`startsWith()`](./String-startsWith.md) |


🔗 Navigation

🔙 [Back to startsWith()](17.md)

⏭️[ Next: trim() →](19.md)