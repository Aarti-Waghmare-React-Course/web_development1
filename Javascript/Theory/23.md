# 🚀 JavaScript `String.prototype.padEnd()`

## 🧩 Summary
In this tutorial, you’ll learn how to use the **JavaScript `padEnd()`** method to **pad the end of a string** with another string until the resulting string reaches a specified length.

---

## 📘 Introduction
The **`padEnd()`** method allows you to **add padding characters to the end** of a string until it reaches a given total length.

### 🧠 Syntax
string.padEnd(targetLength, padString);

📖 Parameters
| Parameter      | Description                                                                                                     |
| -------------- | --------------------------------------------------------------------------------------------------------------- |
| `targetLength` | The final length of the string after padding.                                                                   |
| `padString`    | *(Optional)* The string to pad with. Defaults to `' '` (a single space). If longer than needed, it’s truncated. |

🧩 If targetLength ≤ current string length, the original string is returned unchanged.

👉 To pad from the beginning, use padStart()

💡 Example 1: Basic Usage

The following example pads the string 'ha' from the end until the total length is 10 characters, and 'yahoo' to a total of 20 characters:

let s = 'ha'.padEnd(10, 'ha');
console.log({ s });

s = 'yahoo'.padEnd(20, 'o');
console.log({ s });

✅ Output
{ s: 'hahahahaha' }
{ s: 'yahooooooooooooooooo' }


🧠 Explanation:

'ha' is repeated to fill the total of 10 characters.

'yahoo' is extended with 'o' until the string reaches 20 characters.

💡 Example 2: Formatting Table Data (CLI Example)

When building Command-Line Interface (CLI) tools, you can use padEnd() to neatly align data in table format.

const formatTable = (data, cellWidth = 20) => {
  data.forEach((row) => {
    let formattedRow = row.map((cell) => cell.padEnd(cellWidth)).join('|');
    console.log(formattedRow);
  });
};

let data = [
  ['Name', 'Age', 'City'],
  ['Alice', '30', 'New York'],
  ['Bob', '25', 'San Francisco'],
  ['Charlie', '35', 'Los Angeles'],
];

formatTable(data);

✅ Output
Name                |Age                 |City
Alice               |30                  |New York
Bob                 |25                  |San Francisco
Charlie             |35                  |Los Angeles

🧠 Explanation:
Each cell is padded to 20 characters for perfect column alignment.

🧾 Summary Table
| Feature               | Description                                   |
| --------------------- | --------------------------------------------- |
| 🔹 Method             | `padEnd(targetLength, padString)`             |
| 🧱 Returns            | A **new string** (original remains unchanged) |
| ⬅️ Padding Direction  | Adds characters to the **end (right)**        |
| 🧩 Default Pad String | `' '` (space)                                 |
| 🧠 Related Method     | [`padStart()`](./String-padStart.md)          |


🔗 Navigation

🔙 [Back to padStart()](22.md)

⏭️[Next: JavaScript String split →](24.md)