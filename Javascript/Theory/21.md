# 🚀 JavaScript `String.prototype.trimEnd()`

## 🧩 Summary
In this tutorial, you’ll learn how to use the **JavaScript `trimEnd()`** method to remove whitespace from the **end (trailing side)** of a string.

---

## 📘 Introduction
To remove whitespace from the **end** of a string, JavaScript provides the **`trimEnd()`** method.

### 🧠 Syntax
let newString = str.trimEnd();

🧩 Returns:
A new string (without trailing whitespace).
The original string remains unchanged.


🧾 Whitespace Characters in JavaScript
The following characters are considered whitespace in JavaScript:
| Character | Description     |
| --------- | --------------- |
| `' '`     | Space           |
| `\t`      | Tab             |
| `\r`      | Carriage return |
| `\n`      | Newline         |
| `\v`      | Vertical tab    |
| `\f`      | Form feed       |

👉 To remove whitespace from the beginning of a string, use trimStart()


💡 Example 1: Basic Usage
const str = '   JavaScript   ';
const result = str.trimEnd();

console.log({ str });
console.log({ result });

✅ Output
{ str: '   JavaScript   ' }
{ result: '   JavaScript' }


🧠 Explanation:
trimEnd() removes only the trailing (right-side) whitespace, keeping the leading spaces untouched.


💡 Example 2: Removing Trailing Newlines
const str = 'Hi\n\n'.trimEnd();
console.log({ str });

✅ Output
{ str: 'Hi' }


🧠 Explanation:
Here, trimEnd() removes two newline characters (\n\n) from the end of the string.


💡 Example 3: Chaining with Other String Methods

Since trimEnd() returns a string, you can chain it with other string methods.

let message = 'Hello, World!  ';
let greeting = message.trimEnd().replace('World', 'JavaScript');
console.log(greeting);

✅ Output
Hello, JavaScript!


🧠 Explanation:
First, trimEnd() removes the trailing spaces, then replace() swaps "World" with "JavaScript".

🧩 Alias: trimRight()

The trimRight() method is an alias for trimEnd() — both refer to the same function and behave identically.


However, prefer trimEnd() for these reasons:
| Reason                 | Description                                                                     |
| ---------------------- | ------------------------------------------------------------------------------- |
| 🧱 **Standardization** | `trimEnd()` is part of **ECMAScript**, while `trimRight()` is **non-standard**. |
| ⚙️ **Consistency**     | Matches naming conventions of other methods like `padEnd()`.                    |

✅ Always use trimEnd() for clarity and compatibility.

🧾 Summary Table
| Feature               | Description                                                          |
| --------------------- | -------------------------------------------------------------------- |
| 🔄 Returns new string | Does not modify the original string                                  |
| 🔹 Removes whitespace | Only from the **end (right)**                                        |
| 🧱 Alias              | `trimRight()`                                                        |
| 🧩 Related Methods    | [`trim()`](./String-trim.md), [`trimStart()`](./String-trimStart.md) |


🔗 Navigation

🔙 [Back to trimStart()](20.md)

⏭️ [Next: JavaScript String Padding →](22.md)