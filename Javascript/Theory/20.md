# 🚀 JavaScript `String.prototype.trimStart()`

## 🧩 Summary
In this tutorial, you’ll learn how to use the **JavaScript `trimStart()`** method to remove whitespace from the **beginning** of a string.

---

## 📘 Introduction
To remove whitespace only from the **start** (leading side) of a string, JavaScript provides the **`trimStart()`** method.

### 🧠 Syntax
let newString = str.trimStart();

🧩 Returns:
A new string with leading whitespace removed.
The original string remains unchanged.


🧾 Whitespace Characters in JavaScript
The following characters are considered whitespace:
| Character | Description     |
| --------- | --------------- |
| `' '`     | Space           |
| `\t`      | Tab             |
| `\r`      | Carriage return |
| `\n`      | Newline         |
| `\v`      | Vertical tab    |
| `\f`      | Form feed       |

👉 To remove whitespace from the end of a string, use trimEnd()


💡 Example 1: Basic Usage
const str = '   JavaScript Tutorial   ';
const result = str.trimStart();

console.log({ str });
console.log({ result });

✅ Output
{ str: '   JavaScript Tutorial   ' }
{ result: 'JavaScript Tutorial   ' }

🧠 Explanation:
trimStart() removes only the leading (left-side) whitespace and keeps the trailing spaces intact.


🧩 Alias: trimLeft()

The trimLeft() method is an alias for trimStart(), meaning both work identically.


However, it’s recommended to use trimStart() because:

| Reason                 | Description                                                                      |
| ---------------------- | -------------------------------------------------------------------------------- |
| 🧱 **Standardization** | `trimStart()` is part of **ECMAScript**, while `trimLeft()` is **non-standard**. |
| ⚙️ **Consistency**     | Matches naming style of other methods like `padStart()`.                         |
✅ Therefore, always prefer trimStart() for clarity and consistency.

🧾 Summary Table
| Feature               | Description                                                      |
| --------------------- | ---------------------------------------------------------------- |
| 🔄 Returns new string | Does not modify the original string                              |
| 🔹 Removes whitespace | Only from the **beginning**                                      |
| 🧱 Alias              | `trimLeft()`                                                     |
| 🧩 Related Methods    | [`trim()`](./String-trim.md), [`trimEnd()`](./String-trimEnd.md) |


🔗 Navigation
🔙 [Back to trim()](19.md)

⏭️ [Next: trimEnd() →](21.md)