# 🔘 JavaScript Boolean Type

### 🧠 Summary  
In this tutorial, you’ll learn about the **JavaScript Boolean type**, which has only two literal values: `true` and `false`.

---

## 📖 Introduction to the JavaScript Boolean Type

The **Boolean primitive type** in JavaScript has two literal values:  
✅ `true` and ❌ `false`

Example:

let completed = true;
let running = false;

⚠️ Note: Boolean values are case-sensitive.
true and false are valid boolean literals — but True and False are not boolean values.

🔄 Type Casting to Boolean
JavaScript allows values of other types to be cast (converted) to boolean values.
You can do this explicitly using the built-in Boolean() function.

Example:
let error = 'An error occurred';
let hasError = Boolean(error);
console.log(hasError);

🧩 Output:
true

🧠 How It Works
Declare a variable that holds a string:

let error = 'An error occurred';
Convert it into a boolean value using Boolean():

let hasError = Boolean(error);
Log it:


console.log(hasError); // true
✅ Since 'An error occurred' is a non-empty string, Boolean() returns true.

🗂️ Boolean Conversion Rules
Data Type	       Values converted to true	           Values converted to false
string	             Any non-empty string	              "" (empty string)
number	             A ny non-zero number	                0, NaN
object	             Any non-null object	                 null
undefined	         (not applicable)	                     undefined

💡 These conversions happen automatically in many JavaScript statements, like if, while, and logical operators.

⚙️ Implicit Boolean Conversion Example
The if statement implicitly casts its condition to a boolean value using Boolean().

let error = 'An error occurred';
if (error) {
  console.log(error);
}
🧩 Output:
An error occurred

Because the error variable holds a non-empty string, it evaluates to true, and the console.log() executes.

If you change the value to an empty string, it evaluates to false:

let error = '';
if (error) {
  console.log(error);
}
🧩 Output:
(nothing printed)


🏁 Summary
✅ JavaScript Boolean type has two literal values: true and false.
✅ Use Boolean() to explicitly cast other values to booleans.
✅ JavaScript implicitly converts non-boolean values in logical statements (e.g., if, while).