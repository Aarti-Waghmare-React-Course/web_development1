# 🧠 JavaScript Data Types

## 📘 Summary
In this tutorial, you’ll learn about **JavaScript data types** and their unique characteristics.


JavaScript has the **primitive data types**:
- `null`
- `undefined`
- `boolean`
- `number`
- `string`
- `symbol` (from ES2015)
- `bigint` (from ES2020)

and one **complex data type**:
- `object`


## ⚙️ JavaScript is Dynamically Typed
JavaScript is a **dynamically typed language**, meaning that a variable isn’t bound to a specific type.

let counter = 120;  // number
counter = false;    // boolean
counter = "foo";    // string

----> You can check the type using typeof:

let counter = 120;
console.log(typeof counter); // "number"

counter = false; 
console.log(typeof counter); // "boolean"

counter = "Hi";
console.log(typeof counter); // "string"


🟣 The undefined Type
Has only one value: undefined

When a variable is declared but not initialized, it is undefined.

let counter;
console.log(counter);        // undefined
console.log(typeof counter); // undefined

Even undeclared variables return "undefined" with typeof:

console.log(typeof undeclaredVar); // undefined


⚫ The null Type
Has only one value: null

let obj = null;
console.log(typeof obj); // object  ❗ (known JavaScript bug)
JavaScript defines:

console.log(null == undefined); // true


🔢 The number Type
Represents both integers and floating-point numbers.

let num = 100;
let price = 12.5;
let discount = 0.05;
let price2 = 200.00; // interpreted as integer

Number range and special values:

console.log(Number.MAX_VALUE); // 1.7976931348623157e+308
console.log(Number.MIN_VALUE); // 5e-324

console.log(Number.MAX_VALUE + Number.MAX_VALUE); // Infinity
console.log(-Number.MAX_VALUE - Number.MAX_VALUE); // -Infinity


🧩 NaN (Not a Number)
Represents an invalid number.

console.log('a'/2); // NaN
console.log(NaN/2); // NaN
console.log(NaN == NaN); // false


🧵 The string Type
A string is a sequence of characters wrapped in single (') or double (") quotes.

let greeting = 'Hi';
let message  = "Bye";
let msg = 'I\'m a valid string'; // escaped quote

Strings are Immutable
let str = 'JavaScript';
str = str + ' String'; // creates a new string

Modifying individual characters doesn’t work:

let s = 'JavaScript';
s[0] = 'j';
console.log(s); // "JavaScript"


✅ The boolean Type
Two possible values: true or false

let inProgress = true;
let completed = false;
console.log(typeof completed); // boolean

Type Conversion to Boolean
You can convert other values using Boolean():


Type	     ✅ true	                 ❌ false
string	     non-empty string	       empty string
number	     non-zero, Infinity     	0, NaN
object	     non-null object	        null
undefined	      —	                    undefined



console.log(Boolean('Hi')); // true
console.log(Boolean(''));   // false
console.log(Boolean(20));   // true
console.log(Boolean(0));    // false
console.log(Boolean({foo: 100})); // true
console.log(Boolean(null)); // false


🔰 The symbol Type (ES6)
Used to create unique identifiers.

let s1 = Symbol();
console.log(Symbol() == Symbol()); // false


🧮 The bigint Type (ES2020)
Used for very large integers beyond 2^53 - 1.

let pageView = 9007199254740991n;
console.log(typeof pageView); // "bigint"


🧱 The object Type
An object is a collection of key-value pairs.

let emptyObject = {};

let person = {
  firstName: 'John',
  lastName: 'Doe'
};
Objects can contain nested objects:

let contact = {
  firstName: 'John',
  lastName: 'Doe',
  email: 'john.doe@example.com',
  phone: '(408)-555-9999',
  address: {
    building: '4000',
    street: 'North 1st street',
    city: 'San Jose',
    state: 'CA',
    country: 'USA'
  }
};
Accessing Properties

console.log(contact.firstName); // John
console.log(contact.lastName);  // Doe
console.log(contact.age);       // undefined

console.log(contact['phone']);  // (408)-555-9999
console.log(contact['email']);  // john.doe@example.com


🏁 Key Takeaways
✨ JavaScript Data Types Overview:

Primitive types: string, number, boolean, null, undefined, symbol, bigint

Complex type: object

⚡ JavaScript is dynamically typed — variable types can change at runtime.
🧩 Use typeof to check data types.
🚫 null shows type "object" (a known bug).
📏 Use Number.MAX_VALUE, Infinity, and NaN for numeric limits and special cases.
🔠 Strings are immutable.
💡 Boolean() helps in truthy/falsy evaluations.
🔐 Symbol ensures uniqueness.
💪 BigInt handles massive integer values.
🏗️ Objects organize data as key-value pairs.











