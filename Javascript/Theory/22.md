# 🚀 JavaScript `String.prototype.padStart()`

## 🧩 Summary
In this tutorial, you’ll learn how to use the **JavaScript `padStart()`** method to **pad the beginning of a string** with another string until the resulting string reaches a specified length.

---

## 📘 Introduction
The **`padStart()`** method allows you to **add padding characters to the beginning** of a string until it reaches the desired total length.


### 🧠 Syntax
string.padStart(targetLength, padString);
📖 Parameters
| Parameter      | Description                                                                                                       |
| -------------- | ----------------------------------------------------------------------------------------------------------------- |
| `targetLength` | The final length of the resulting string after padding.                                                           |
| `padString`    | *(Optional)* The string to pad with. Defaults to a space `' '`. If it’s longer than needed, it will be truncated. |

🧩 If targetLength is less than or equal to the original string length, the original string is returned unchanged.

👉 To pad the end of a string, use padEnd()


💡 Example 1: Basic Usage

The following example pads '0' to a string until it reaches 5 characters:

const results = ['120', '242', '10'].map((str) => {
  return str.padStart(5, '0');
});

console.log({ results });

✅ Output
{ results: [ '00120', '00242', '00010' ] }


🧠 Explanation:
Each string is padded with '0' on the left until it becomes 5 characters long.


💡 Example 2: Formatting Numbers

You can use padStart() to format numbers so that they have a consistent length, which is useful for things like invoice numbers.

const invoiceNumbers = [1, 12, 123, 1234, 12345];

const formattedInvoiceNumbers = invoiceNumbers.map((no) => {
  return no.toString().padStart(8, '0');
});

console.log(formattedInvoiceNumbers);

✅ Output
[ '00000001', '00000012', '00000123', '00001234', '00012345' ]


🧠 Explanation:
Each invoice number is converted to a string and padded with zeros on the left to make it 8 digits long.


💡 Example 3: Aligning Text in Console Output

When creating CLI applications, padStart() can help align text neatly.

let items = ['Apple', 'Banana', 'Cherry'];
items.forEach((item) => {
  console.log(item.padStart(20, '.'));
});

✅ Output
...............Apple
..............Banana
..............Cherry


🧠 Explanation:
Each fruit name is padded with dots on the left until the total string length is 20, aligning them visually in a column.


🧾 Summary Table
| Feature               | Description                                      |
| --------------------- | ------------------------------------------------ |
| 🔹 Method             | `padStart(targetLength, padString)`              |
| 🧱 Returns            | A new string (original string remains unchanged) |
| ⬅️ Padding Direction  | Adds characters to the **beginning (left)**      |
| 🧩 Default Pad String | `' '` (space)                                    |
| 🧠 Related Method     | [`padEnd()`](./String-padEnd.md)                 |


🔗 Navigation

🔙 [Back to trimEnd()](21.md)

⏭️ [Next: JavaScript String padEnd →](23.md)