# 🚀 JavaScript `String.prototype.trim()`

## 🧩 Summary
In this tutorial, you’ll learn how to use the **JavaScript `trim()`** method to remove whitespace characters from both ends of a string.

---

## 📘 Introduction
The **`String.prototype.trim()`** method returns a **new string** with whitespace removed from both the beginning and the end of the original string.

### 🧠 Syntax
let newString = str.trim();

⚠️ Note:
trim() does not modify the original string — it returns a new one.


🧾 Whitespace Characters in JavaScript
The following characters are considered whitespace:

| Character | Description     |
| --------- | --------------- |
| `' '`     | Space           |
| `\t`      | Tab             |
| `\r`      | Carriage return |
| `\n`      | Newline         |
| `\v`      | Vertical tab    |
| `\f`      | Form feed       |


👉 To remove whitespace from only one side of a string:

Use trimStart() → removes leading whitespace

Use trimEnd() → removes trailing whitespace


💡 Example 1: Basic Usage
let str = '  JavaScript trim  ';
let result = str.trim();

console.log({ str });
console.log({ result });

✅ Output
{ str: '  JavaScript trim  ' }
{ result: 'JavaScript trim' }

🧠 Explanation:
trim() removes the spaces from both ends of the string, returning a new clean string.


💡 Example 2: Removing Newlines
let str = '\nJavaScript Tutorial\n\n';
let result = str.trim();

console.log({ str });
console.log({ result });

✅ Output
{ str: '\nJavaScript Tutorial\n\n' }
{ result: 'JavaScript Tutorial' }


🧠 Explanation:
All leading and trailing newline (\n) characters are trimmed, leaving only the content.


💡 Example 3: Chaining trim() with Other Methods
let name = '  John Doe ';
let [firstName, lastName] = name.trim().split(' ');
console.log({ firstName, lastName });

✅ Output
{ firstName: 'John', lastName: 'Doe' }


🧠 Explanation:
trim() removes the spaces, then split(' ') divides the string into an array of words.


🧾 Summary Table
| Feature               | Description                                                                |
| --------------------- | -------------------------------------------------------------------------- |
| 🔄 Returns new string | Does not modify the original string                                        |
| ⚙️ Removes whitespace | From both start and end                                                    |
| 🧱 Can be chained     | With other string methods                                                  |
| 🔍 Related Methods    | [`trimStart()`](./String-trimStart.md), [`trimEnd()`](./String-trimEnd.md) |


🔗 Navigation

🔙 [Back to endsWith()](18.md)

⏭️ [Next: trimStart() →](20.md)