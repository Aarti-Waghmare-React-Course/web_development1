# 🚀 JavaScript `String.prototype.startsWith()`

## 🧩 Summary
In this tutorial, you’ll learn how to use the **JavaScript `String.startsWith()`** method to perform a **case-sensitive search** and determine whether a string starts with a specific substring.

---

## 📘 Introduction
The **`startsWith()`** method checks whether a string **begins** with the specified substring.

string.startsWith(searchString [, position]);

🧠 Parameters:
searchString → The substring to search for.

position (optional) → The index within the string at which to begin the search. Defaults to 0.


⚡ Behavior:
Returns true if the string starts with the given substring.

Returns false otherwise.

Always performs a case-sensitive comparison.

Throws a TypeError if searchString is a regular expression.


💡 Example 1: Basic Usage
const title = "Jack and Jill Went Up the Hill";
const result = title.startsWith("Jack");

console.log({ result });
✅ Output
{ result: true }

🧠 Explanation:
The string "Jack and Jill Went Up the Hill" starts with "Jack", so the result is true.


💡 Example 2: Case-Sensitive Search
const title = "Jack and Jill Went Up the Hill";
const result = title.startsWith("jack");

console.log({ result });
❌ Output
{ result: false }

🧠 Explanation:
The method is case-sensitive, so "jack" and "Jack" are treated as different strings.


💡 Example 3: Using the position Parameter
const title = "Jack and Jill Went Up the Hill";
const result = title.startsWith("Jill", 9);

console.log({ result });
✅ Output
{ result: true }

🧠 Explanation:
The method starts searching from index 9, where "Jill" begins.


💡 Example 4: Throws a TypeError with RegExp
const str = "JavaScript";
const result = str.startsWith(/J/);

console.log({ result });
⚠️ Output
TypeError: First argument to String.prototype.startsWith must not be a regular expression

🧠 Explanation:
The startsWith() method does not accept regular expressions as arguments.


🧾 Summary
| Feature                    | Description                                  |
| -------------------------- | -------------------------------------------- |
| ✅ Returns `true` / `false` | Checks if the string starts with a substring |
| 🔠 Case-sensitive          | `"Jack"` ≠ `"jack"`                          |
| ⚙️ Optional `position`     | Defines start index for search               |
| ⚡ Throws `TypeError`       | When given a RegExp argument                 |


🔗 Navigation

🔙 [Back to includes()](16.md)

⏭️ [Next: endsWith() →](18.md)